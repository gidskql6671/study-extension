

const commitBlocks = document.body.querySelectorAll('.ContributionCalendar-day')

if (commitBlocks.length > 0) {
  // commitCalendar.style.visibility = 'hidden'
  // style="fill:rgb(0,0,255)"

  commitBlocks
  .forEach(commitBlock => {
    // commitBlock.dataset.level = 4
  })
}